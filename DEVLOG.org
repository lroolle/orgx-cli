#+TITLE: orgx-cli Development Log
#+STARTUP: overview

* [2025-12-27] Code Review Fixes + Architecture Decisions            :BREAKING:RELIABILITY:
:PROPERTIES:
:ID: devlog-2025-12-27-code-review
:END:

** Context
orgx-cli is a Go CLI tool for working with org-mode and markdown files, inspired by gh-cli's design. Context-efficient interface for LLMs to work with structured documents.

** Why
NAK code review identified critical bugs in =set= command: non-TTY safety gate missing, fuzzy heading matching, non-deterministic tag sorting, global string replacement causing corruption.

** What
- =BREAKING= Non-TTY safety: writes require =--yes= in non-interactive mode (pkg/cmd/set/set.go, pkg/cmd/heading/set/set.go)
- =RELIABILITY= Position-based heading lookup using =Span.Start= from parser instead of =strings.Contains= (pkg/cmd/heading/shared/lookup.go)
- =RELIABILITY= Line-based editing at specific index instead of =strings.Replace= (pkg/cmd/set/set.go, pkg/cmd/heading/set/set.go)
- Tag sorting with =slices.Sort()= for deterministic output (pkg/cmd/heading/set/set.go)
- Removed misleading =--fields= flag that was accepted but non-functional (pkg/cmdutil/json_flags.go)
- Fixed loose ref matching: =strings.Contains= → =strings.HasSuffix= (pkg/cmd/heading/shared/lookup.go)
- Added ~/path expansion via new =ExpandPath()= helper (pkg/cmd/heading/shared/lookup.go)
- Eliminated double file reads: new =ParseFileContent()= and =FindHeadingFromContent()= (pkg/parser/parser.go)

** How
*** Steps
1. Added TTY check in write commands with =io.CanPrompt()= guard
2. Modified heading lookup to use =heading.Span.Start= from parser AST
3. Converted global string replacement to line-index-based editing using =findHeadingLine()=
4. Applied =slices.Sort()= to tag slices before serialization
5. Removed unused =--fields= flag definition and help text
6. Changed ref suffix matching from substring to proper suffix check
7. Added home directory expansion using =os.UserHomeDir()= + =filepath.Join()=
8. Refactored parser interface to accept =[]byte= content directly

*** Commands
#+BEGIN_SRC bash
# Before: silent writes in non-TTY mode (DANGEROUS)
orgx set file.org::/Path --todo DONE < /dev/null  # would silently write

# After: requires explicit confirmation
orgx set file.org::/Path --todo DONE --yes < /dev/null  # safe
#+END_SRC

*** Diffs
#+BEGIN_SRC diff
--- a/pkg/cmd/heading/set/set.go
+++ b/pkg/cmd/heading/set/set.go
@@ -85,6 +85,11 @@ func setRun(opts *Options) error {
        }

+       if !opts.DryRun && !opts.Yes && !opts.IO.CanPrompt() {
+               return cmdutil.NewFlagError("--yes required in non-interactive mode")
+       }
+
        // Use position-based lookup instead of title matching
-       doc, heading, err := shared.FindHeading(origPath, selector)
+       doc, heading, err := shared.FindHeadingFromContent(content, selector)

--- a/pkg/cmd/heading/shared/lookup.go
+++ b/pkg/cmd/heading/shared/lookup.go
@@ -15,7 +15,7 @@ func MatchesSelector(ref, selector string) bool {
                return true
        }
        // Check suffix match for more specific selectors
-       return strings.Contains(ref, selector)
+       return strings.HasSuffix(ref, selector)
 }
#+END_SRC

*** Data/Schema Notes                                         :SCHEMA:MIGRATION:
N/A - no schema changes

*** API Contract                                                     :BREAKING:
- =orgx set= now errors in non-interactive mode without =--yes= flag
- Previously silently wrote files in pipelines/scripts
- Migration: add =--yes= to all non-interactive invocations

** Result
*** Outcome
All NAK review findings resolved. =set= command now safe for non-interactive use, deterministic output, correct heading resolution.

*** Metrics                                                             :PERF:
N/A - correctness fixes, no performance measurements

*** Tests
- Added: N/A (fixes to existing code)
- Updated: pkg/cmd/set/set_test.go (validation for new --yes requirement)
- Coverage delta: +0% (existing tests, behavior corrections)

*** Observability
N/A - CLI tool, no runtime observability

** Decisions
| Decision                     | Alternatives                      | Rationale                                                      | DRI      | Timestamp (UTC)     |
|------------------------------+-----------------------------------+----------------------------------------------------------------+----------+---------------------|
| No implicit ID writes        | Auto-add IDs on read commands     | Reads must remain pure; mutations explicit only                | lroolle  | 2025-12-27T00:00:00 |
| Noun-verb grammar (gh-style) | Verb-only (git-style)             | Better namespacing, clearer for agents, aliases for humans     | lroolle  | 2025-12-27T00:00:00 |
| Three-tier ref stability     | Binary (stable/unstable)          | Allows read-only use of fragile refs with warnings             | lroolle  | 2025-12-27T00:00:00 |
| Position-based heading match | Title-based fuzzy matching        | Prevents wrong heading edits when duplicate titles exist       | lroolle  | 2025-12-27T00:00:00 |
| Line-index editing           | AST regeneration / global replace | Preserves formatting, predictable behavior                     | lroolle  | 2025-12-27T00:00:00 |

** Risks                                                            :SECURITY:
| Risk                          | Mitigation                                  | Owner   | Status   |
|-------------------------------+---------------------------------------------+---------+----------|
| Breaking scripts using =set=  | Document migration, error msg points to fix | lroolle | Accepted |
| Markdown line number mismatch | Document limitation, org-files prioritized  | lroolle | Accepted |

** Rollback
*** Trigger
- Users report cannot run =set= in scripts despite =--yes= flag
- Heading edits fail systematically with "not found" errors
- Tag ordering changes break downstream parsers

*** Procedure
1. =git revert= commits in pkg/cmd/set/, pkg/cmd/heading/set/, pkg/cmd/heading/shared/
2. Data impact: none (CLI tool, no persistent state)

** WIP
- [ ] Implement =orgx id ensure= command for adding IDs to headings without them
- [ ] Add fragility warnings to write commands using non-ID refs (::/, ::H:)
- [ ] Wire up noun-verb command structure via Cobra command groups
- [ ] Consolidate duplicate set.go files (pkg/cmd/set/ vs pkg/cmd/heading/set/)
- [ ] Add =orgx heading= subcommands: list, view, set (noun-verb)
- [ ] Add =orgx file= subcommands: parse, outline, validate (noun-verb)
- [ ] Add =orgx link= subcommands: list, backlinks, broken (noun-verb)

** Links
- Issues/PRs: N/A (pre-release)
- Artifacts: N/A
- Docs/Runbooks: /Users/eric/wrk/src/github.com/lroolle/WIP/251219_orgx-init/worktree/orgx-cli/CLAUDE.md
- Review: NAK code review (verbal)

** Notes
*** Assumptions
- go-org library uses 1-based line numbers (confirmed via testing)
- Markdown parser stores byte offsets in =Span=, not line numbers (known limitation)
- org files are primary use case; markdown support secondary

*** Constraints
- Must maintain zero external runtime dependencies (stdlib + go-org + goldmark only)
- Must preserve exact file formatting (whitespace, comments, non-heading content)
- Output must be deterministic for agent parsability

*** Open Questions
- Should =orgx id ensure= generate UUIDs or short hashes? (UUIDs chosen for org-roam compat)
- How to handle ID conflicts across files? (Error on duplicate, let user resolve)
- Markdown heading hash stability: include children in hash? (Yes, for stability)

*** Gotchas
- go-org AST uses 1-based line numbers, Go file I/O uses 0-based indexing
- Duplicate heading titles break title-based matching (now fixed with position-based)
- Map iteration order is random in Go (tags now explicitly sorted)
- =strings.Replace(s, old, new, -1)= replaces ALL occurrences (now line-scoped)

** Architecture: Core Contracts Established
*** Five Immutable Rules
1. =READS NEVER WRITE= - no implicit mutations during get/peek/find
2. =WRITES ARE EXPLICIT= - require =--yes= in non-interactive mode
3. =REFS HAVE POLICIES= - stable (::ID:) vs fragile (::/path, ::H:hash)
4. =OUTPUT IS DETERMINISTIC= - sorted, stable schema, no randomness
5. =GRAMMAR IS NOUN-VERB= - =orgx <noun> <verb>= with legacy aliases

*** Ref Stability Hierarchy
| Level     | Pattern              | Example                    | Write Policy      |
|-----------+----------------------+----------------------------+-------------------|
| STABLE    | =path::ID:<uuid>=    | =file.org::ID:abc-123=     | Safe, encouraged  |
| FRAGILE   | =path::/outline=     | =file.org::/Proj/Design=   | Warn, allow       |
| FRAGILE   | =path::H:<hash>=     | =README.md::H:a1b2c3=      | Warn, allow       |
| AMBIGUOUS | bare path            | =file.org=                 | Error, reject     |

*** Noun-Verb Taxonomy
#+BEGIN_SRC text
orgx <noun> <verb> [target] [flags]

file:
  - list      (stats for .org/.md files in dir)
  - parse     (full IR output)
  - outline   (structure only, alias: peek)
  - validate  (lint, check refs)

heading:
  - list      (all headings with filters)
  - view      (single heading, alias: get)
  - set       (modify heading, alias: set)
  - tree      (hierarchical view)

id:
  - ensure    (add IDs to headings missing them)
  - list      (all IDs in workspace)
  - check     (validate uniqueness)

link:
  - list      (outgoing links from file/heading)
  - backlinks (incoming links to target)
  - broken    (dead refs)

ws:
  - add       (register workspace)
  - list      (show all workspaces)
  - use       (set default)
  - show      (current workspace config)
#+END_SRC

*** Legacy Agent Aliases (preserved for backward compat)
- =peek= → =file outline=
- =get= → =heading view=
- =set= → =heading set=
- =find= → =heading list=
- =ls= → =file list=

** Technical Debt
*** TODO Consolidate duplicate set.go files
Two implementations exist:
- =pkg/cmd/set/set.go= (root-level command)
- =pkg/cmd/heading/set/set.go= (noun-verb subcommand)

Recommendation: deprecate root-level, migrate to noun-verb.

*** TODO Add integration tests for non-TTY mode
Current tests use =iostreams.Test()= which simulates TTY. Need explicit non-TTY test cases.

*** TODO Document markdown line number limitation
Markdown parser (goldmark) provides byte offsets, not line numbers. Current workaround: scan file to convert offset → line. Document as known limitation.
